# This file exists for compatibility when license_engine is run as a standalone app.
# When mounted as an engine in a host application, the host manages all
# environment-specific configuration. Settings here are intentionally omitted.
Rails.application.configure do
end
